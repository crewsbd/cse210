/*
-Menu(of activities)
-Each activity has a start message: Name, Description, Duration prompt(seconds), pause for several seconds then start.
-Common ending message("Good job") -> Pause -> State activity duration -> pause -> finish
-Pause is animated \|/-, > -> --> ->
-See video
-Actities (classes?): reflection, breathing and enumeration. See doc
*/

using System;

class Program
{
    static void Main(string[] args)
    {


    }
}